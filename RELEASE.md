测试版（Pre-release），Docker 镜像 `ghcr.io/fe-spark/ecohub:v2.5.0-beta.2`。

### 核心变更

#### 1. 采集任务停止即时取消写入队列
- **快速响应中止**：在手动停止单个任务（`StopTask`）或全部任务（`StopAllTasks`）时，立即清空并取消写入调度器（`collectWriteScheduler`）中积压的待写入页面任务（`cancelSource`），杜绝任务中止后后台仍继续向数据库刷盘阻塞后续任务的现象。

#### 2. 全局优先海报图源（Poster Source）
- **图源持久化与时序解耦**：支持将任意采集站标记为全局海报图源（`is_poster_source`，全局单选互斥）。海报源采集时将其高清封面及幻灯图持久化写入 `movie_poster` 表并增量覆盖主站；主站采集时亦优先反查应用海报源的高清图，彻底解耦时序依赖。
- **管理后台卡片状态展示**：采集站卡片直观增加「海报源」高亮徽标，编辑弹窗支持开关切换。

#### 3. 快照构建与清理性能重构（支持 20w+ 影片）
- **大事务解耦**：全量重构快照 `RebuildFilmListSnapshot` 移除外层 MySQL 全局大事务，改为分批独立写入，大幅减轻大体量数据下的 Undo Log 压力与锁等待风险。
- **平滑分批清理**：旧快照清理 `pruneOldFilmListSnapshots` 升级为 5000 条 Chunk 循环删除，避免单次超大 DELETE 锁死表空间。

#### 4. 采集中心交互与状态解耦优化
- **卡片时长纯内存管理**：卡片上的采集时长仅作为单次触发采集的临时入参，切换时不再发起持久化写库请求，避免覆盖站点实体配置。
- **快捷开关状态保护**：修复单站快捷启停接口未继承海报源属性的问题。
