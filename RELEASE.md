测试版 **v2.4.0-beta.4**，Docker 镜像 `ghcr.io/fe-spark/ecohub:v2.4.0-beta.4`。

### 升级指引

- **从已有版本升级**：拉取镜像 `ghcr.io/fe-spark/ecohub:v2.4.0-beta.4` 或配置 docker-compose 测试升级。本版本为测试版，不会覆盖 `:latest`。

---

### 核心变更

1. **片库检索：无二级分类也能筛**
   - 短剧等没有子类的大类保留剧情 / 地区 / 年份与排序，不再把别的大类标签串过来。
   - 筛选项只在快照发布时生成，分类页 GET 不再写库。

2. **采集：部分失败会收尾，不再误标已停止**
   - 连续失败但已有入库时进入「部分失败，等待收尾」，停按钮与编辑删除按收尾态禁用。
   - 用户点停止仍显示「已停止」，单站收尾结束也不会变成「采集完成」。

3. **访问分析：仅超管、重启还在、按日滚动**
   - 菜单与接口仅超级管理员可看。
   - 启动不再清空 `EcoHub:Access:*`；日聚合 14 天 TTL，明细列表定长裁剪。
   - 已闭合的天每小时滚入 MySQL 日表（每天一行 + 热搜/热播 Top10），到期删除，库不会随 QPS 涨。今天的曲线和 recent 仍走 Redis。
