正式版 **v2.2.3**，Docker 镜像 `ghcr.io/fe-spark/ecohub:v2.2.3` 与 `ghcr.io/fe-spark/ecohub:latest`。

### 升级指引

- **从 v2.1.x / v2.2.x 升级**：执行 `docker compose pull && docker compose up -d` 即可（或后台「检查更新」一键平滑升级）。
- **从 v2.1.0 之前升级**：请先按 [v2.1.0 说明](https://github.com/fe-spark/EcoHub/releases/tag/v2.1.0) 完成素材卷目录迁移，再升级到本版本。

---

### v2.2.3 核心变更

#### 1. 网站设置分卡片独立配置与局部更新
- **服务端接口拆分与独立存储**：网站配置拆分为基本信息 (`/api/config/basic`)、赞赏配置 (`/api/config/tip`)、站点公告 (`/api/config/notice`) 三组独立读写接口，避免全量更新覆盖其他模块配置。
- **管理端网站设置卡片化重构**：将网站设置重构拆分为「网站基本信息」、「赞赏配置」、「站点公告」三个独立卡片组件，各卡片支持独立保存与重置。

#### 2. 开屏公告多端投放与前台弹窗
- **公告多端投放与版本控制**：公告数据模型支持 `showInWeb` / `showInApp` 多端独立投放开关，并向前兼容旧版 `version` 字段与编解码。
- **公告卡片交互与实时预览**：后台公告卡片支持 Web / App 投放平台勾选、版本号过滤以及弹窗实时预览功能。
- **前台公共公告弹窗**：前台公共布局集成 `NoticeModal` 公告弹窗，基于 `useSyncExternalStore` 与 sessionStorage 实现会话级已读静默。

#### 3. 采集与系统体验优化
- **源站清理与重试保护**：源站改地址级联物理清理历史失败记录，重试次数增加封顶保护并在服务启动时自动纠偏（#16）。
- **引导巡检与通知治理**：修复 React 19 引导巡检组件中 ref render 访问及状态同步问题，优化系统通知展示及交互。

#### 4. 多端生态与文档更新
- **文档更新**：README 与相关文档补充 Telegram 官方交流群入口。
- **鸿蒙客户端同步**：同步更新鸿蒙客户端 `app-for-ohos` 子模块至 `v1.0.6`。

