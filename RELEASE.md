测试版 **v2.1.2-beta.6**，镜像 `ghcr.io/fe-spark/ecohub:v2.1.2-beta.6`，**不会**覆盖 `:latest`。

已部署 **v2.1.0** 及以上：把 compose 镜像改成 `ghcr.io/fe-spark/ecohub:v2.1.2-beta.6` 后执行 `docker compose pull && docker compose up -d`。正式版后台不会把 beta 当作可升级版本。

从 **v2.1.0 之前** 升级的，请先按 [v2.1.0 说明](https://github.com/fe-spark/EcoHub/releases/tag/v2.1.0) 做完素材卷迁移，再升到本版本。

### 本版本变更

- **采集链路 429 限流自适应防御**：引入单站限流动态平滑放行与退避冷却机制，遭遇上游 429 限流时自动平缓放行节奏，并提供专属 6 次容错重试保障，大幅降低高并发下上游限流造成的丢页漏采（连续失败达到 10 次仍会安全熔断）。
- **采集核心模块化重构**：将采集核心彻底解耦拆分为生命周期管理、分页拉取、进度维护、错误自愈与分批写入等单一职责子模块，大幅提升采集流水线稳定性与扩展性。
- **真实网络与 HTTP 错误透传**：全面重构分类树、分页数与详情接口的错误透传链路，拒绝模糊的空响应掩盖，精准输出 DNS 解析、连接超时与 HTTP 状态码等底层根因。
- **默认主采集源高可用调整**：全新安装预设金鹰1(JY)作为默认主采集站，并补充速博、U酷等优质采集源，避免初始化与分类同步阻塞（已有部署不自动迁移）。
- **空数据引导与前台健壮性增强**：全新部署且无影片内容时，前台首页自动呈现初始接入与采集引导；完善站点名称（`siteName`）全局兜底机制，杜绝前端空标。
- **日志净化与凭证常驻保护**：精准过滤正常未登录访问触发的 `redis.Nil` 孤立日志；优化启动缓存清理逻辑，保留用户与管理员 Token 登录凭证，服务重启不再导致在线用户掉线。
