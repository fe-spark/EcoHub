测试版 **v2.4.0-beta.8**，Docker 镜像 `ghcr.io/fe-spark/ecohub:v2.4.0-beta.8`。

### 升级指引

- **从已有版本升级**：拉取镜像 `ghcr.io/fe-spark/ecohub:v2.4.0-beta.8` 或配置 docker-compose 测试升级。本版本为测试版，不会覆盖 `:latest`。

---

### 核心变更

1. **访问分析耗时图改为时序热力**
   - 去掉散点图。按时间 × 耗时档（≤50 / 50–200 / 200–500 / >500ms）着色，密集请求不再叠成一团。
   - 仍可切换「柱状分布」。
