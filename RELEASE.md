正式版 **v2.4.0**，Docker 镜像 `ghcr.io/fe-spark/ecohub:v2.4.0` 与 `ghcr.io/fe-spark/ecohub:latest`。

### 升级指引

- **从已有版本升级**：执行 `docker compose pull && docker compose up -d` 即可（或后台「检查更新」一键平滑升级）。
- **配置说明**：本次新增「访问分析」模块，依赖 Redis 进行内存计数与实时聚合，请确保 Redis 服务正常连接。MySQL 表结构（新增 `access_dailies` 表）将在启动时由 GORM 自动平滑迁移。

---

### v2.4.0 核心变更

#### 1. 全新上线：全链路访问分析与监控体系（Access Analytics）
- **全站流量与埋点分析**：
  - 前台页面（首页、片库筛选、分类搜索、播放页、搜索页等）全面集成静默 PV/UV 访问跟踪。
  - 后端新增请求访问日志中间件，支持高延迟（>500ms）及异常（4xx/5xx）请求智能采样，并自动过滤 SSR 静态资源干扰。
- **热度榜单与极速缓存**：
  - 提供实时热门搜索词、热门播放影片 Top 200 统计，集成影片详情极速缓存，秒级召回热度数据。
- **数据归档与按日滚动聚合（Rollup）**：
  - 数据在 Redis 中进行高并发聚合，定时落库归档至 MySQL `access_dailies` 数据表，自动滚动保留最近 14 天数据并安全清理过期数据。
- **管理后台「访问分析」可视化看板**：
  - **核心指标看板**：直观呈现 PV、UV、今日请求量、P95 延迟与错误率等关键运行状态。
  - **时序趋势与维度分析**：提供流量时序趋势折线图、设备/终端与路由分类占比环形图。
  - **耗时时序热力图**：创新采用时间 × 耗时档（≤50ms / 50–200ms / 200–500ms / >500ms）热力着色网格，并支持无缝切换柱状分布图。
  - **明细日志与安全隔离**：提供实时访问明细日志检索与分页；严格限制仅超级管理员（Role=1）拥有访问分析查看与数据接口权限。

#### 2. 采集系统稳定性与收尾机制强化
- **采集进度收尾机制优化**：修复采集任务在部分页面网络异常或空数据时，后台进度无法置为完成/失败卡在 `processing` 状态的问题，保障采集生命周期可靠闭环。
- **采集源卡片与健康度展示**：重构管理后台采集源卡片的状态标签、耗时展示与实时进度条。

#### 3. 片库检索与多级分类规则重构
- **无子分类一级分类支持**：修复无子分类的一级分类在片库检索筛选面板报错的问题，重构规则工作区与筛选快照机制。
- **多端筛选面板联动**：优化移动端筛选抽屉与桌面端筛选面板的分类联动体验。

#### 4. 多端生态与子模块同步
- **鸿蒙客户端（EcoHub for OHOS）**：同步更新鸿蒙子模块至 `v1.1.2-beta.1`。
- **Flutter 客户端（EcoHub for Android）**：同步更新 Android / iOS 子模块至最新实现（更新播放器与模块能力）。

#### 5. 公共端与交互体验优化
- **Header 搜索交互重构**：Header 搜索入口改为图标触发，分类导航项根据屏幕可用宽度自适应展示，放不下的分类自动收纳。
