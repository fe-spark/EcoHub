# 附属站搜索方案

主站不存在的片也要能搜到、能播。**不要落库**，直接打采集源自己的 MacCMS 接口（项目里已经有直连：`GetVodDirectBySource`）。

不写 `film_index`，不给未匹配建卡片表，不改孤儿清理。未匹配 playlist 仍是暂存，24h 对不上 `movie_match_key` 会被清掉——搜索和播放都不依赖它。

---

## 0. 为什么必须走 CMS 接口

| 做法 | 问题 |
|---|---|
| 搜本地快照 | 只有主站已收录的片 |
| 把未匹配 playlist 当目录 | 定时孤儿清理会删；也没有独立增量更新 |
| 附属站写 `film_index` | 破坏主站身份 / 分类 / 切主站 |

采集源本身就是 MacCMS：`ac=list&wd=` 搜索，`ac=detail&ids=` 取播放地址。TVBox 直连已经在用。前台搜索复用同一条请求，解析成现有卡片/播放结构。

---

## 1. 效果

```text
搜关键词
  ├─ Tab「聚合」     本地快照（现状，秒开，只含已收录）
  ├─ Tab「主站」     打主站 CMS wd=（含尚未采进库的）
  └─ Tab「附属站 X」 打该源 CMS wd=（主站没有的也能出）

点卡片
  ├─ 已能对上本地 mid → /play?id={mid}&source={sourceId}          现有播放页（切源 + 相关推荐）
  └─ 对不上           → /play/live?source={sourceId}&sid={vod_id} 现场播放页（只播当前采集源）
```

现场播放**只读源站、不写库**。播完即走，不进快照、不进孤儿表。没有相关推荐，也不能切到其它采集源（同一源站若有多条线路，只显示线路胶囊）。旧链 `/play?source=&sid=` 重定向到 `/play/live`。

---

## 2. 搜索 API

`GET /searchFilm?keyword=&source=&current=&sort=`

| `source` | 行为 |
|---|---|
| 空 | 现有本地模糊检索 |
| `film_sources.id` | 该源 `Uri`：`ac=list&wd={keyword}&pg={current}` |

响应仍是 `{ list, page, sort }`，加 `sources`：

```json
{
  "list": [],
  "page": { "current": 1, "pageSize": 12, "total": 45, "pageCount": 4 },
  "sort": "",
  "sources": [
    { "id": "", "name": "聚合", "count": 80 },
    { "id": "<主站id>", "name": "<主站名>" },
    { "id": "<附属站id>", "name": "<附属站名>" }
  ]
}
```

`sources` 来自已启用采集源。聚合请求就能带上 Tab 名单。前端拿到名单后**立刻并发**打每个源的 `ac=list&wd=`，Tab 上直接显示 loading，完成则显示该源 `total`。切 Tab 走缓存，不再请求。

### list 卡片

`MovieBasicInfo` 加两个字段：

```text
id         能对上本地则为主站 mid，否则 0
sourceId   当前 Tab 的 film_sources.id（聚合填主站 id）
sourceMid  源站 vod_id；id=0 时必填，进播用
```

对上本地的规则（有就用，没有就现场播）：

- 附属站：`movie_source_mapping` 的 `(source_id, source_mid) → global_mid`
- 主站：本地快照已有 `mid == vod_id`

### CMS 搜索实现

复用现有直连，不要另写一套 HTTP：

1. `FindCollectSourceById`，未启用则空列表
2. `ApiGet`：`ac=list`、`wd`、`pg`（与 `GetVodDirectBySource` 相同）
3. 解析 `FilmListPage`（采集已有模型）
4. 超时单独设短（建议 8s）。失败 / 空包 → `list=[]`，不要把整个搜索打成 Failed
5. 聚合接口只打本地。前端拿到 `sources` 后并发打全部源站第 1 页；切 Tab 用缓存。分页仍按当前源请求

`sort` 对 CMS Tab 不生效（源站搜没有这套排序）。聚合 Tab 仍用现有 sort。

---

## 3. 播放 API

扩展 `GET /filmPlayInfo`，响应结构不变（`detail / current / currentPlayFrom / currentEpisode / relate`）。

| 参数 | 说明 |
|---|---|
| `id` | 本地 mid；现场播可不传或 `0` |
| `playFrom` | 现有，URL 的 `source` 填这里 |
| `episode` | 现有 |
| `sid` | **新增**，源站 `vod_id` |

```text
id > 0          → GetFilmDetail(mid) → resolvePlayableSourceID（现状）
否则 source+sid → 该源 ac=detail&ids={sid}
                  JsonCollect / ConvertFilmDetail（采集已有）
                  套该源 DomainReplaceRules
                  合成 MovieDetailVo（List 只有这一源）
                  不写库、不走 FilmPlayInfoKey:{mid} 缓存
                  relate = []
```

源站挂了或解析失败：返回「暂无影片信息」，与现在空片一致。

Web 现场播走独立页 `/play/live`，仍调这个接口（`id=0` + `playFrom` + `sid`）。不要把空的 `relate` 和切站栏塞进主站播放页。

---

## 4. 进播（三端）

| 卡片 | 路径 | 页面 |
|---|---|---|
| `id > 0` | `/play?id={mid}&source={sourceId}` | 现有播放页：切源、相关推荐 |
| `id = 0` | `/play/live?source={sourceId}&sid={sourceMid}` | 现场播放页：播放器 + 选集 + 简介 |

- Web：`resolvePlayEntryPath` 无 mid 时进 `/play/live`。原 `/play` **只处理 mid**，UI 不改；旧链 `/play?source=&sid=` 仅重定向到 `/play/live`。
- Android / 鸿蒙：`openPlay` 按同一规则分流。`id>0` 进原播放页；无 mid 进独立现场播放页（布局对齐自定义播放页：播放器 + 线路胶囊 + 选集 + 简介，无相关推荐、无切站）。历史 `sourceId:sid` 也进现场页。

现场页不拉相关推荐、不提供采集源切换。同一 CMS 源若 `list` 有多条线路，只保留线路胶囊。

历史：有 mid 仍用 mid；无 mid 用 `sourceId:sid`，禁止 `id=0`。收藏沿用播放页现有逻辑。

---

## 5. 改哪些文件

```text
server/internal/service/                   按源 CMS 搜索；sid 现场合成播放详情
server/internal/handler/index_handler.go   source / sid
web/src/app/(public)/search/               源 Tab；并发拉源；loading/count
web/src/app/(public)/play/live/            现场播放页（无相关推荐、无切站）
web/src/app/(public)/play/page.tsx         仅 mid；无 mid 则跳 /play/live（不改播放页 UI）
web/src/lib/playNavigation.ts              mid → /play；否则 → /play/live
app-for-android/lib/pages/search_page.dart 源 Tab
app-for-android/lib/pages/live_play_page.dart 现场播放页（复用自定义播放页布局）
app-for-android/lib/utils/nav_util.dart    mid → /play；否则 → /play/live
app-for-ohos/.../SearchPage.ets            源 Tab
app-for-ohos/.../LivePlayPage.ets          现场播放页（复用自定义播放页布局）
app-for-ohos/.../NavUtil.ets               mid → PlayPage；否则 → LivePlayPage
```

采集、孤儿清理、快照、分类、首页、TVBox 常规列表不动。`GetVodDirectBySource` 保持给 TVBox 吐原始 JSON，前台搜索在 service 里解析，不把源站原始包丢给浏览器。

---

## 6. 验收

1. 聚合 Tab 与现在一致，不打源站。
2. 主站库里没有、附属站 CMS 有的片：打开该源 Tab 能看到；点进去进现场播放页能播，无相关推荐、无切站（Web `/play/live`，Android `/play/live`，鸿蒙 `LivePlayPage`）。
3. 已匹配的片：源 Tab 里 `id` 为本地 mid，进播走现有 `/play`（App `PlayPage`）并选中该源。
4. 源站超时 / 禁用：该 Tab 空列表，其它 Tab 不受影响。
5. 播现场片之后，库表不新增 `film_index` / snapshot / 未匹配 playlist。
6. 孤儿清理、切主站行为不变。

---

## 7. 明确不做

- 未匹配结果写入任何业务表
- 改孤儿清理、给未匹配开豁免
- 为 CMS 结果再造内存模糊索引（源站搜什么就展示什么）
- 把现场播放塞进主站播放页（空相关推荐 / 空切站）
- 把 TVBox 原始 CMS JSON 直接当搜索接口给前台
